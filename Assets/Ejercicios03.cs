using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicios03 : MonoBehaviour
{
    public string texto;

    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Hola " + texto);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
